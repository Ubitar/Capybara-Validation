<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ubitar.validator">

    <application>
        <provider
            android:name=".provider.AppProvider"
            android:authorities="${applicationId}.validator.provider" />
    </application>
</manifest>